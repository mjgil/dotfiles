#!/usr/bin/env bash

# This script is a wrapper to run the install-local.sh script
# It's created to allow running with ./install_local.sh as requested

# Run the actual installation script
./install-local.sh "$@"
